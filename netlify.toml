[build]
  command = "npm install"
  functions = "netlify/functions" # Directory for serverless functions
  publish = "." # Root directory for static files (if any)

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api" # Point to the 'api' function
  status = 200
  force = true